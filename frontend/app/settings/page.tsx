export default function SettingsPage() {
  return (
    <div className="space-y-6">
      <div>
        <p className="text-sm uppercase tracking-[0.24em] text-accent">Settings</p>
        <h1 className="mt-2 text-3xl font-semibold">Workspace settings</h1>
      </div>
      <div className="rounded-lg border bg-card p-6 text-sm text-muted">
        Settings placeholder for exchange keys, watchlists, indicator defaults, and AI provider configuration.
      </div>
    </div>
  );
}
